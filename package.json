{
  "name": "lunch-alegra-monorepo",
  "version": "1.0.0",
  "description": "Monorepo para el proyecto Lunch Alegra - Microservicios y Frontend",
  "private": true,
  "scripts": {
    "install:microservices": "cd microservices && pnpm install",
    "install:frontend": "cd frontend && pnpm install",
    "install:all": "npm run install:microservices && npm run install:frontend",
    
    "dev:infra": "cd microservices/infra && docker-compose up -d",
    "dev:infra:down": "cd microservices/infra && docker-compose down",
    "dev:infra:logs": "cd microservices/infra && docker-compose logs -f",
    "dev:microservices": "npm run dev:infra && cd microservices && pnpm -w install && pnpm install && pnpm run build:shared && pnpm -F inventory-svc run migrate",
    "dev:frontend": "cd frontend && pnpm run dev",
    "dev:all": "concurrently \"npm run dev:microservices\" \"npm run dev:frontend\"",
    
    "prod:up": "cd microservices/infra && docker-compose -f docker-compose.prod.yml up -d --build",
    
    "docker:clean": "docker system prune -f && docker volume prune -f",
    "docker:clean:all": "docker system prune -a -f && docker volume prune -f"
  },
  "workspaces": [
    "microservices",
    "frontend"
  ],
  "keywords": [
    "microservices",
    "frontend",
    "monorepo",
    "lunch-alegra"
  ],
  "author": "Victor Alvarez",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^6.0.1"
  }
}
