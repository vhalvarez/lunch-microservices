# Entorno
NODE_ENV=development

# Infra (solo dev)
AMQP_URL=amqp://guest:guest@localhost
DATABASE_URL=postgres://postgres:postgres@localhost:5432/lunchday
REDIS_URL=redis://localhost:6379

# Integración externa
MARKET_URL=https://recruitment.alegra.com/api/farmers-market

# Tunables generales
RMQ_PREFETCH=200

# market-adapter-svc
MARKET_MAX_ATTEMPTS=5
MARKET_CONCURRENCY=128
MARKET_BASE_BACKOFF_MS=150
MARKET_HTTP_CONN=64

# inventory-svc · Reconciler
RECONCILER_MAX_RETRIES=5
RECONCILER_EVERY_MS=20000
RECONCILER_BASE_DELAY_MIN=1
RECONCILER_BATCH_LIMIT=300
RECONCILER_BASE_SECONDS=3

# order-svc · BFF
ORDER_BATCH_SIZE=100
BFF_PORT=4000

# predictor-svc · AI Predictions
ANALYSIS_WINDOW_HOURS=1
MIN_ORDERS_BATCH=10
DEBOUNCE_MS=5000
FORCE_ANALYSIS_INTERVAL_MS=60000
CLEANUP_INTERVAL_MS=3600000
KEEP_PREDICTIONS_COUNT=100

# ============================================
# Groq
# ============================================
# Regístrate en: https://console.groq.com
# No requiere tarjeta de crédito
# Límites generosos: 30 req/min, 14,400 req/día

GROQ_ENABLED=true
GROQ_API_KEY=gsk_your_free_api_key_here
GROQ_MODEL=llama3-70b-8192
